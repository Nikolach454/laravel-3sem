# Документация проекта Laravel - Работа с маршрутизацией и Blade-шаблонами

## 📋 Содержание
1. [Описание проекта](#описание-проекта)
2. [Структура проекта](#структура-проекта)
3. [Маршрутизация (Routes)](#маршрутизация-routes)
4. [Blade-шаблонизатор](#blade-шаблонизатор)
5. [Bootstrap 5](#bootstrap-5)
6. [Как запустить проект](#как-запустить-проект)

---

## Описание проекта

Это учебный проект, демонстрирующий работу с:
- **Laravel Framework** - современный PHP-фреймворк
- **Системой маршрутизации (Routes)** - организация URL-адресов приложения
- **Blade Templates** - шаблонизатор Laravel для создания представлений
- **Bootstrap 5** - CSS-фреймворк для адаптивного дизайна

### Структура сайта:
- **Header** - шапка с навигационным меню
- **Main** - основной контент (главная, о нас, контакты)
- **Footer** - подвал с информацией об авторе

---

## Структура проекта

```
laravel_3sem/
├── routes/
│   └── web.php                          # Определение маршрутов
├── resources/
│   └── views/
│       ├── layouts/
│       │   └── app.blade.php           # Главный макет (обертка)
│       ├── home.blade.php              # Страница приветствия
│       ├── about.blade.php             # Страница "О нас"
│       └── contacts.blade.php          # Страница "Контакты"
└── public/
    └── index.php                        # Точка входа
```

---

## Маршрутизация (Routes)

### Файл: `routes/web.php`

Маршруты определяют, какая страница (представление) должна отображаться при обращении к определенному URL.

### Главная страница

```php
Route::get('/', function () {
    return view('home');
})->name('home');
```

**Как это работает:**
- `Route::get('/', ...)` - обрабатывает GET-запрос к корневому URL (`/`)
- `return view('home')` - возвращает представление `home.blade.php`
- `->name('home')` - присваивает маршруту имя `home` для удобной ссылки

### Страница "О нас"

```php
Route::get('/about', function () {
    return view('about');
})->name('about');
```

**Как это работает:**
- URL: `/about`
- Отображает статическую страницу из файла `about.blade.php`
- Имя маршрута: `about`

### Страница "Контакты" (с динамическими данными)

```php
Route::get('/contacts', function () {
    // Создаем массив с контактами
    $contacts = [
        [
            'type' => 'address',
            'title' => 'Адрес',
            'value' => 'г. Москва, ул. Примерная, д. 123',
        ],
        // ... другие контакты
    ];

    // Передаем данные в представление
    return view('contacts', [
        'contacts' => $contacts,
        'socialMedia' => $socialMedia,
        'workingHours' => $workingHours
    ]);
})->name('contacts');
```

**Как это работает:**
1. Создаются массивы с данными (`$contacts`, `$socialMedia`, `$workingHours`)
2. Метод `view()` принимает два параметра:
   - Имя представления (`'contacts'`)
   - Массив данных, которые будут доступны в шаблоне
3. В Blade-шаблоне эти данные доступны как переменные

**Передача данных в представление:**
- Первый способ (используется в проекте):
  ```php
  return view('contacts', ['contacts' => $contacts]);
  ```
- Альтернативный способ:
  ```php
  return view('contacts')->with('contacts', $contacts);
  ```

---

## Blade-шаблонизатор

Blade - это мощный шаблонизатор Laravel, который позволяет создавать переиспользуемые компоненты.

### 1. Главный макет (Layout)

**Файл:** `resources/views/layouts/app.blade.php`

Это основной шаблон, который содержит общую структуру для всех страниц:

```blade
<!DOCTYPE html>
<html lang="ru">
<head>
    <title>@yield('title', 'Мой сайт')</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
    <!-- Шапка с навигацией -->
    <header>
        <nav class="navbar navbar-expand-lg">
            <!-- Меню навигации -->
        </nav>
    </header>

    <!-- Основной контент -->
    <main>
        @yield('content')
    </main>

    <!-- Подвал -->
    <footer>
        @yield('footer', 'Иванов Иван Иванович, Группа ИС-301')
    </footer>
</body>
</html>
```

**Ключевые директивы Blade:**

#### `@yield('название')`
Определяет секцию, которая будет заполнена контентом из дочерних шаблонов.

```blade
@yield('content')           <!-- Обязательная секция -->
@yield('title', 'Default')  <!-- С значением по умолчанию -->
```

#### Навигационное меню
```blade
<a class="nav-link {{ request()->routeIs('home') ? 'active' : '' }}"
   href="{{ route('home') }}">
    Главная
</a>
```

**Объяснение:**
- `{{ route('home') }}` - генерирует URL по имени маршрута
- `request()->routeIs('home')` - проверяет, активен ли данный маршрут
- `? 'active' : ''` - добавляет класс `active` для текущей страницы

### 2. Дочерние шаблоны

Все страницы наследуют макет и заполняют его секции.

**Пример:** `resources/views/home.blade.php`

```blade
@extends('layouts.app')

@section('title', 'Главная страница')

@section('content')
    <h1>Добро пожаловать!</h1>
    <p>Контент главной страницы</p>
@endsection
```

**Директивы:**
- `@extends('layouts.app')` - наследует макет
- `@section('title', 'Текст')` - устанавливает значение секции (краткая форма)
- `@section('content')` ... `@endsection` - определяет блок контента

### 3. Работа с данными в Blade

**Файл:** `resources/views/contacts.blade.php`

#### Вывод переменных
```blade
{{ $variable }}  <!-- Экранированный вывод (безопасный) -->
{!! $html !!}    <!-- Неэкранированный вывод (для HTML) -->
```

#### Цикл @foreach
```blade
@foreach($contacts as $contact)
    <div>
        <h5>{{ $contact['title'] }}</h5>
        <p>{{ $contact['value'] }}</p>
    </div>
@endforeach
```

**Как это работает:**
1. Laravel передает массив `$contacts` из маршрута
2. `@foreach` перебирает каждый элемент массива
3. Внутри цикла доступны все поля элемента (`$contact['title']`, `$contact['value']`)

#### Условные конструкции @if
```blade
@if(isset($contact['description']))
    <small>{{ $contact['description'] }}</small>
@endif
```

```blade
@if($contact['type'] == 'email')
    <svg><!-- Иконка email --></svg>
@elseif($contact['type'] == 'phone')
    <svg><!-- Иконка телефона --></svg>
@else
    <svg><!-- Другая иконка --></svg>
@endif
```

#### Проверка существования данных
```blade
@if(isset($socialMedia) && count($socialMedia) > 0)
    <!-- Отображаем социальные сети -->
    @foreach($socialMedia as $social)
        <a href="{{ $social['url'] }}">{{ $social['name'] }}</a>
    @endforeach
@endif
```

---

## Bootstrap 5

### Подключение Bootstrap

В файле `layouts/app.blade.php`:

```html
<!-- CSS -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet">

<!-- JavaScript -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js">
</script>
```

### Основные компоненты Bootstrap в проекте

#### 1. Navbar (Навигация)
```html
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
        <a class="navbar-brand" href="/">Мой сайт</a>
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link active" href="/">Главная</a>
            </li>
        </ul>
    </div>
</nav>
```

**Классы:**
- `navbar` - основной класс навигации
- `navbar-expand-lg` - адаптивное меню (сворачивается на малых экранах)
- `navbar-dark bg-primary` - темная тема с синим фоном
- `container` - центрирование контента

#### 2. Grid System (Сетка)
```html
<div class="row">
    <div class="col-lg-8">Основной контент (8 колонок)</div>
    <div class="col-lg-4">Боковая панель (4 колонки)</div>
</div>
```

**Как работает:**
- `row` - создает горизонтальный ряд
- `col-lg-8` - занимает 8 из 12 колонок на больших экранах
- Система автоматически адаптируется под размер экрана

#### 3. Cards (Карточки)
```html
<div class="card shadow-sm">
    <div class="card-body">
        <h5 class="card-title">Заголовок</h5>
        <p class="card-text">Текст карточки</p>
    </div>
</div>
```

**Классы:**
- `card` - контейнер карточки
- `shadow-sm` - легкая тень
- `card-body` - внутренняя область с отступами

#### 4. Buttons (Кнопки)
```html
<a href="#" class="btn btn-primary btn-lg">Большая кнопка</a>
<button class="btn btn-outline-success">Обведенная кнопка</button>
```

#### 5. Utilities (Утилиты)
```html
<!-- Отступы -->
<div class="mb-4">margin-bottom: 1.5rem</div>
<div class="p-5">padding: 3rem</div>

<!-- Текст -->
<p class="text-muted">Серый текст</p>
<p class="text-center">Центрированный текст</p>
<h1 class="fw-bold">Жирный шрифт</h1>

<!-- Цвета -->
<div class="bg-light">Светлый фон</div>
<span class="text-primary">Синий текст</span>
```

---

## Как запустить проект

### 1. Требования
- PHP >= 8.1
- Composer
- Laravel 10.x

### 2. Установка зависимостей
```bash
composer install
```

### 3. Настройка окружения
```bash
cp .env.example .env
php artisan key:generate
```

### 4. Запуск сервера разработки
```bash
php artisan serve
```

### 5. Открыть в браузере
```
http://localhost:8000
```

---

## Основные концепции

### Маршрутизация
**Что это?** Система, которая определяет, какой код выполнить при обращении к определенному URL.

**Зачем?** Организует структуру приложения и связывает URL с логикой.

**Пример:**
```
URL: /about → Маршрут → Контроллер/Функция → Представление (about.blade.php)
```

### Blade-шаблоны
**Что это?** Система шаблонов для создания HTML с динамическим контентом.

**Преимущества:**
- Переиспользование кода (layouts)
- Чистый синтаксис
- Автоматическое экранирование данных (защита от XSS)
- Мощные директивы (@if, @foreach, @include)

### Передача данных в представления
**Как работает:**
1. В маршруте создаем данные (массив, объект)
2. Передаем через `view('название', ['ключ' => $данные])`
3. В Blade-шаблоне используем `{{ $ключ }}`

---

## Полезные ссылки

- [Документация Laravel](https://laravel.com/docs)
- [Документация Blade](https://laravel.com/docs/blade)
- [Документация Bootstrap 5](https://getbootstrap.com/docs/5.3)
- [Маршрутизация Laravel](https://laravel.com/docs/routing)

---

## Автор
**Иванов Иван Иванович**
Группа: ИС-301
Дата: Ноябрь 2025

---

## Выводы

В ходе выполнения работы были изучены:

1. ✅ **Маршрутизация** - создание и именование маршрутов
2. ✅ **Blade-шаблонизатор** - наследование, секции, директивы
3. ✅ **Передача данных** - работа с массивами в представлениях
4. ✅ **Bootstrap 5** - создание адаптивного интерфейса
5. ✅ **Структура Laravel** - организация файлов и папок

Проект демонстрирует базовые принципы работы с Laravel и может служить основой для более сложных приложений.
