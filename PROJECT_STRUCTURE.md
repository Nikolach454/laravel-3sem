# Структура проекта

## 📂 Файловая структура

```
laravel_3sem/
│
├── routes/
│   └── web.php                          # ⚙️ Маршрутизация приложения
│
├── resources/
│   └── views/
│       ├── layouts/
│       │   └── app.blade.php           # 🎨 Главный макет (обертка)
│       │
│       ├── home.blade.php              # 🏠 Главная страница
│       ├── about.blade.php             # ℹ️ Страница "О нас"
│       └── contacts.blade.php          # 📞 Страница "Контакты"
│
├── EXPLANATION.md                       # 📖 Подробная документация
├── QUICK_START.md                       # 🚀 Быстрый старт
└── PROJECT_STRUCTURE.md                 # 📁 Этот файл
```

---

## 🔄 Поток данных в приложении

```
Браузер (URL)
    ↓
routes/web.php (Маршрут)
    ↓
Closure функция / Controller
    ↓
Создание данных (массивы)
    ↓
view('название', ['данные'])
    ↓
Blade-шаблон (resources/views/)
    ↓
HTML отправляется в браузер
```

---

## 📄 Описание файлов

### 1. `routes/web.php`
**Назначение:** Определение всех маршрутов приложения

**Содержит:**
- `GET /` → home (главная страница)
- `GET /about` → about (страница "О нас")
- `GET /contacts` → contacts (страница "Контакты" с данными)

**Ключевые концепции:**
```php
Route::get('url', function() {
    // Логика
})->name('имя');
```

---

### 2. `resources/views/layouts/app.blade.php`
**Назначение:** Главный макет (обертка) для всех страниц

**Содержит:**
- Header с навигационным меню
- Секцию `@yield('content')` для основного контента
- Footer с информацией об авторе
- Подключение Bootstrap 5

**Используемые Blade-директивы:**
- `@yield('название')` - создание секций
- `{{ route('имя') }}` - генерация URL
- `{{ request()->routeIs('имя') }}` - проверка активного маршрута

---

### 3. `resources/views/home.blade.php`
**Назначение:** Главная страница с приветствием

**Структура:**
- Hero-блок с приветствием
- Карточка с описанием проекта
- Три карточки с особенностями (Laravel, Blade, Bootstrap)

**Blade-директивы:**
- `@extends('layouts.app')` - наследование макета
- `@section('title')` - установка заголовка
- `@section('content')` - основной контент

---

### 4. `resources/views/about.blade.php`
**Назначение:** Статическая страница "О нас"

**Содержит:**
- История проекта
- Цели обучения
- Технологический стек
- Боковая панель с быстрыми ссылками

**Bootstrap компоненты:**
- Cards (карточки)
- List groups (списки)
- Grid system (сетка)
- Badges (значки)

---

### 5. `resources/views/contacts.blade.php`
**Назначение:** Динамическая страница "Контакты"

**Получает данные:**
- `$contacts` - массив контактов
- `$socialMedia` - массив социальных сетей
- `$workingHours` - режим работы

**Blade-директивы:**
- `@foreach` - цикл по массиву
- `@if/@elseif/@else` - условные операторы
- `@isset` - проверка существования переменной
- `{{ $variable }}` - вывод данных

**Пример использования:**
```blade
@foreach($contacts as $contact)
    <h5>{{ $contact['title'] }}</h5>
    <p>{{ $contact['value'] }}</p>
@endforeach
```

---

## 🎯 Как работают Blade-шаблоны

### Наследование

```blade
<!-- layouts/app.blade.php -->
<html>
    <body>
        @yield('content')  <!-- Место для контента -->
    </body>
</html>

<!-- home.blade.php -->
@extends('layouts.app')  <!-- Наследуем макет -->

@section('content')       <!-- Заполняем секцию -->
    <h1>Привет!</h1>
@endsection
```

### Результат

```html
<html>
    <body>
        <h1>Привет!</h1>
    </body>
</html>
```

---

## 🔗 Связь между файлами

1. **Браузер** запрашивает `/contacts`
2. **routes/web.php** получает запрос и создает данные
3. **Данные** передаются в `view('contacts', [...])`
4. **contacts.blade.php** получает данные и расширяет `layouts/app.blade.php`
5. **app.blade.php** оборачивает контент в общий макет
6. **HTML** отправляется браузеру

---

## 🎨 Bootstrap 5 компоненты в проекте

| Компонент | Где используется | Назначение |
|-----------|------------------|------------|
| Navbar | app.blade.php | Навигационное меню |
| Grid (row/col) | Все страницы | Адаптивная сетка |
| Cards | home, about, contacts | Блоки контента |
| Buttons | home, contacts | Кнопки и ссылки |
| List groups | about, contacts | Списки |
| Table | contacts | Таблица режима работы |
| Badges | about | Цветные метки |

---

## 📝 Редактирование контента

### Изменить текст в футере
**Файл:** `resources/views/layouts/app.blade.php`

Найдите:
```blade
@yield('footer', 'Иванов Иван Иванович, Группа ИС-301')
```

Замените на свои данные:
```blade
@yield('footer', 'Ваше ФИО, Группа XXX-XXX')
```

---

### Добавить новую страницу

1. **Создать маршрут** в `routes/web.php`:
```php
Route::get('/new-page', function () {
    return view('new-page');
})->name('new.page');
```

2. **Создать представление** `resources/views/new-page.blade.php`:
```blade
@extends('layouts.app')

@section('title', 'Новая страница')

@section('content')
    <h1>Моя новая страница</h1>
@endsection
```

3. **Добавить ссылку в меню** в `layouts/app.blade.php`:
```blade
<li class="nav-item">
    <a class="nav-link" href="{{ route('new.page') }}">
        Новая страница
    </a>
</li>
```

---

### Изменить контактные данные

**Файл:** `routes/web.php`

Найдите массив `$contacts` в маршруте `/contacts` и измените значения:
```php
$contacts = [
    [
        'type' => 'address',
        'title' => 'Адрес',
        'value' => 'Ваш новый адрес',  // ← Здесь
    ],
    // ...
];
```

---

## ✅ Чек-лист выполненных задач

- ✅ Создан макет (обертка) для страниц сайта
- ✅ В навигации расположены ссылки "О нас" и "Контакты"
- ✅ Сверстана страница-приветствие
- ✅ Сверстана страница "О нас"
- ✅ Реализована маршрутизация для всех страниц
- ✅ Для страницы "Контакты" реализован массив данных
- ✅ Данные отправлены методом view()
- ✅ Страница "Контакты" отображает динамические данные
- ✅ Визуальная часть создана с использованием Bootstrap 5
- ✅ Header с меню
- ✅ Footer с ФИО и группой
- ✅ Создана документация в формате Markdown

---

## 📚 Дополнительная информация

Для подробного изучения каждой концепции смотрите:
- **EXPLANATION.md** - детальное объяснение работы проекта
- **QUICK_START.md** - быстрый запуск проекта
